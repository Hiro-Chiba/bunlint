# bunlint 用の環境変数サンプル
# このファイルをコピーして .env.local などにリネームし、実際の値を設定してください。

# Neon などで発行した Postgres の接続文字列を設定します。
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB_NAME"

# Google AI Studio で取得した Gemini API キーを設定します。
GEMINI_API_KEY="your-gemini-api-key"
# GEMINI_MODEL="gemini-2.0-flash-lite"   # 必要に応じてモデルを上書き
# GEMINI_API_VERSION="v1beta,v1"         # 利用したいAPIバージョンをカンマ区切りで指定
